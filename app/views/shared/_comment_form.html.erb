<%= form_for(@comment) do |f| %>
    <%= render 'shared/error_messages', object: f.object %>
    <div class="field">
      <%= f.text_area :content, placeholder: "Compose new micropost...", size:"24x1" %>
    </div>
    <%= f.submit "Post", class: "btn btn-large btn-primary" %>
<% end %>